---
import AuthLayout from "../layouts/AuthLayout.astro";
import { LoginForm } from "@/components/auth/LoginForm";
---

<AuthLayout title="Witaj ponownie" subtitle="Zaloguj się, aby kontynuować pracę z fiszkami" hideAuthButtons>
  <section class="grid w-full gap-8 lg:grid-cols-[1.2fr_1fr]">
    <article
      class="hidden flex-col justify-center gap-6 rounded-3xl border border-white/15 bg-gradient-to-br from-white/10 via-purple-400/10 to-transparent p-10 text-white shadow-[0_35px_110px_-60px_rgba(56,189,248,0.8)] backdrop-blur-2xl lg:flex"
    >
      <h2 class="text-3xl font-semibold tracking-tight">Twoja wiedza w jednym miejscu</h2>
      <p class="text-base leading-relaxed text-white/80">
        Generuj fiszki z notatek, artykułów i wykładów. Dzięki personalizowanym pakietom ucz się szybciej i skuteczniej.
      </p>
      <ul class="space-y-3 text-sm text-white/70">
        <li class="flex items-center gap-2">
          <span class="size-2 rounded-full bg-[#ff3ecf] shadow-[0_0_0_6px_rgba(255,62,207,0.25)]" aria-hidden="true"></span>
          Chronione dane i dostęp tylko dla Ciebie.
        </li>
        <li class="flex items-center gap-2">
          <span class="size-2 rounded-full bg-[#27e0ff] shadow-[0_0_0_6px_rgba(39,224,255,0.25)]" aria-hidden="true"></span>
          Historia generacji, podgląd zmian i szybkie edycje.
        </li>
        <li class="flex items-center gap-2">
          <span class="size-2 rounded-full bg-[#9dff6b] shadow-[0_0_0_6px_rgba(157,255,107,0.25)]" aria-hidden="true"></span>
          Automatyczne limity i kontrola nad postępami nauki.
        </li>
      </ul>
    </article>

    <div class="flex w-full justify-center">
      <LoginForm client:load className="w-full" />
    </div>
  </section>
</AuthLayout>
