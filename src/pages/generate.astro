---
import Layout from "../layouts/Layout.astro";
import GenerateView, { ENABLE_AI_GENERATION } from "../components/generation/GenerateView";

export const prerender = false;
---

<Layout title={ENABLE_AI_GENERATION ? "Generowanie fiszek AI" : "Tworzenie fiszek"}>
  <GenerateView client:load />
</Layout>
